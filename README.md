🏷️НАЗВАНИЕ ПРОДУКТА

Автоматический скрипт резервного копирования PostgreSQL в Docker-окружении

ℹ️ОПИСАНИЕ

Скрипт автоматизирует создание сжатых резервных копий базы данных PostgreSQL, запущенной в Docker-контейнере через Docker Compose. Разработан для защиты критически важных данных Drupal-сайта от потери при сбоях. 

⭐Ключевые особенности:
- Автоматическое определение времени создания бэкапа через временную метку
- Сжатие дампов с помощью gzip для экономии места
- Ротация архивов: удаление файлов старше 7 дней
- Проверка целостности созданных бэкапов
- Диагностика ошибок с автоматическим удалением поврежденных файлов
- Безопасная работа только при наличии запущенного контейнера БД

🆚ОСОБЕННОСТИ ПЕРЕД АНАЛОГАМИ

В отличие от стандартных решений:
1) Полная интеграция с Docker Compose без необходимости ручного указания хостов/портов
2) Автоматическая проверка типа СУБД перед созданием дампа
3) Детальное логирование ошибок с выводом конкретных причин сбоя
4) Двухуровневая валидация: проверка кода возврата + анализ размера файла
5) Защита от удаления старых бэкапов при неудачном создании нового

📋ТРЕБОВАНИЯ
- Docker Engine версии 20.10+
- Docker Compose v2.10+
- Запущенный контейнер PostgreSQL с доступом через сервис "db"
- Клиентские утилиты PostgreSQL (pg_dump) внутри контейнера БД

⚙️УСТАНОВКА
1. Поместите скрипт backup_script.sh в директорию с вашим docker-compose.yml
2. Настройте параметры подключения в начале скрипта согласно вашему docker-compose.yml:
   DB_SERVICE - имя сервиса БД (по умолчанию "db")
   DB_USER - имя пользователя PostgreSQL
   DB_PASSWORD - пароль пользователя
   DB_NAME - имя базы данных
3. Установите права на выполнение:
   chmod +x backup_script.sh

🚀ИСПОЛЬЗОВАНИЕ

Быстрый запуск:
./backup_script.sh

Пример вывода при успехе:
Успешно создан бэкап: backup_20251225_143045.sql.gz (24K)

Автоматизация через cron (ежедневный бэкап в 02:00):
0 2 * * * /путь/к/backup_script.sh >> /var/log/db_backup.log 2>&1

🔄ВОССТАНОВЛЕНИЕ ИЗ БЭКАПА
1. Распакуйте архив: gunzip < backup_XXXXXX.sql.gz | docker-compose exec -T db psql -U kirillLABS -d kirillLABS
2. Для полного восстановления остановите Drupal перед операцией

🔒БЕЗОПАСНОСТЬ

ВАЖНО: Хранение паролей в открытом виде в скрипте небезопасно! Для продакшена:
1. Используйте .pgpass файл внутри контейнера БД
2. Передавайте параметры через переменные окружения:
   DB_PASSWORD=$(docker secret ls | grep db_password | awk '{print $2}')
3. Ограничьте права доступа: chmod 600 backup_script.sh

💻РАЗРАБОТКА

Для улучшения скрипта:
1. Создайте issue с описанием проблемы/предложения
2. Сделайте fork репозитория
3. Реализуйте изменения в отдельной ветке
4. Отправьте pull request с детальным описанием изменений

Проверка работоспособности после изменений:
./backup_script.sh && echo "Тест успешен" || echo "Ошибка в строке $LINENO"

⚠️ИЗВЕСТНЫЕ ОГРАНИЧЕНИЯ
- Требует запущенного контейнера БД перед выполнением
- Не поддерживает шифрование резервных копий "из коробки"
- Для очень больших БД (>1GB) требуется дополнительная настройка таймаутов

👥УЧАСТНИКИ

Основной разработчик:
- Kych3r 
